name: band
description: "A production-ready Mi Smart Band 6 application."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.34.4
  flutter_secure_storage: ^9.2.2
  pointycastle: ^3.9.1
  provider: ^6.1.2
  permission_handler: ^11.3.1
  flutter_foreground_task: ^8.15.0
  shared_preferences: ^2.3.3
  path_provider: ^2.1.5
  fl_chart: ^0.69.0
  google_fonts: ^6.2.1
  record: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
